package contract

import "testing"

// TestBlueprintPlaceholder is a placeholder until blueprint contract tests
// (list_matching_paths, list_tree, zones) are added in T004–T006.
func TestBlueprintPlaceholder(t *testing.T) {
	t.Skip("blueprint contract tests added in T004–T006")
}
